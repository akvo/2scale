import {
    Easing,
    TextStyle,
    backgroundColor,
    Color,
    Icons,
} from "./chart-style.js";
import echarts from "echarts";

const Maps = (title, subtitle, data, extra) => {
    return {};
};

export default Maps;
